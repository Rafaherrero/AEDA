#include <vector>
#include <cstdlib>
#pragma once

using namespace std;

template<class tipo_vector>
class algoritmos{
public:
    algoritmos(void);
    algoritmos(unsigned size);
    ~algoritmos(void);


};